BASE: "https://202.122.38.207:8443"
NAME: "IHEP-SATOSA-OIDC-Test"
STATE_ENCRYPTION_KEY: "IHEP_SECRET_123"
USER_ID_HASH_SALT: "IHEP_SALT_123"

INTERNAL_ATTRIBUTES: "/opt/satosa/etc/internal_attributes.yaml"
COOKIE_STATE_NAME: "satosa_state"
COOKIE_SECURE: true
COOKIE_HTTPONLY: true
COOKIE_SAMESITE: None

BEHIND_REVERSE_PROXY: true

PLUGIN_PATHS:
  - "/opt/satosa/plugins/frontends"
  - "/opt/satosa/plugins/backends"

CUSTOM_PLUGIN_MODULE_PATHS:
  - "/opt/satosa/plugins/micro_services"

FRONTEND_MODULES:
  - "/opt/satosa/plugins/frontends/saml2_frontend.yaml"
  - "/opt/satosa/plugins/frontends/oidc_frontend.yaml"

BACKEND_MODULES:
  - "/opt/satosa/plugins/backends/saml2_backend.yaml"

MICRO_SERVICES:
  - "/opt/satosa/plugins/micro_services/normalize_attributes.yaml"

LOGGING:
  version: 1
  disable_existing_loggers: false
  formatters:
    default:
      format: "%(asctime)s %(name)s %(levelname)s %(message)s"
  handlers:
    console:
      class: logging.StreamHandler
      formatter: default
      level: DEBUG
  root:
    level: DEBUG
    handlers: [console]
